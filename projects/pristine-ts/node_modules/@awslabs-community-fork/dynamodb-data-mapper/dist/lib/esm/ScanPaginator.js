import { buildScanInput } from './buildScanInput';
import { Paginator } from './Paginator';
import { ScanPaginator as BasePaginator } from '@awslabs-community-fork/dynamodb-query-iterator';
/**
 * Iterates over each page of items returned by a DynamoDB scan until no more
 * pages are available.
 */
export class ScanPaginator extends Paginator {
    constructor(client, itemConstructor, options = {}) {
        super(new BasePaginator(client, buildScanInput(itemConstructor, options), options.limit), itemConstructor);
    }
}
//# sourceMappingURL=ScanPaginator.js.map