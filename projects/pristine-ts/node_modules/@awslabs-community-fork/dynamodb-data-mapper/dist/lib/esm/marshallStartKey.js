import { marshallValue } from '@awslabs-community-fork/dynamodb-data-marshaller';
/**
 * @internal
 */
export function marshallStartKey(schema, startKey) {
    const key = {};
    for (const propertyName of Object.keys(startKey)) {
        const propSchema = schema[propertyName];
        const { attributeName = propertyName } = propSchema;
        if (propSchema) {
            key[attributeName] = marshallValue(propSchema, startKey[propertyName]);
        }
    }
    return key;
}
//# sourceMappingURL=marshallStartKey.js.map