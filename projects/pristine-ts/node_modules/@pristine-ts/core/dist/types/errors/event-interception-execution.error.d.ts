import { LoggableError } from "@pristine-ts/common";
import { Kernel } from "../kernel";
/**
 * This Error is thrown when an error happens in the execution of an event interceptor.
 */
export declare class EventInterceptionExecutionError extends LoggableError {
    constructor(message: string, event: any, kernel: Kernel, error?: Error);
}
