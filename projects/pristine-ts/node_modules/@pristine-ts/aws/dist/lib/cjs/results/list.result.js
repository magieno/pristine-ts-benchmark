"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ListResult = void 0;
/**
 * The list of items returned when a list query is called on DynamoDB.
 * It implements IterableIterator so that you can iterate over the items as if they were in a regular list.
 */
class ListResult {
    /**
     * The list of items returned when a list query is called on DynamoDB.
     * @param _items The items returned by the query.
     * @param _paginationResult The pagination results of the query.
     */
    constructor(_items, _paginationResult) {
        this._items = _items;
        this._paginationResult = _paginationResult;
        this.index = 0;
        this.done = false;
    }
    [Symbol.iterator]() {
        return this;
    }
    next() {
        if (this.done) {
            return {
                done: this.done,
                value: undefined,
            };
        }
        if (this.index === this._items.length) {
            this.done = true;
            return {
                done: this.done,
                value: this._items[this.index],
            };
        }
        const value = this._items[this.index];
        this.index += 1;
        return {
            done: false,
            value,
        };
    }
    get paginationResult() {
        return this._paginationResult;
    }
    get items() {
        return this._items;
    }
}
exports.ListResult = ListResult;
//# sourceMappingURL=list.result.js.map